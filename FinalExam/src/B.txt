3．关于抽象类，下面说法正确的是(  )。
20．实现Runnable接口和继承Thread类并覆盖其中的(  )方法可以用来创建一个新线程。
7．人为抛出异常对象e的语句为(  )。
19．Statement对象的作用是(  )。
1．Java语言的特点是( )。
11．Java事件处理三要素包括事件、(  )、(  )。
4．用于定义接口的关键字是(  )。
14．所有字节流的类都是(  )类和OutputStream类的子类。
